/**
 * basket.js — Basket builder UI
 * Agent/UI owns this file.
 *
 * Must register: window.Arbicart.basket = { getItems() → [{name, qty}] }
 */

window.Arbicart = window.Arbicart || {};

// Agent/UI implements this file
