/**
 * savings.js — Savings calculation + display
 * Agent/map owns this file.
 *
 * Must register: window.Arbicart.savings = { showSavings(homeZip, pricesByZip) }
 */

window.Arbicart = window.Arbicart || {};

// Agent/map implements this file
